import Stripe from "stripe";

type Props = {
  subscription: Stripe.Subscription | undefined
}

export const CancelSubscription = ({ subscription }: Props) => {
  return (
    <div>
      
    </div>
  );
}